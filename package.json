{
  "name": "battery-bar",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:syntax": "node --check dist/battery_bar.js"
  }
}
